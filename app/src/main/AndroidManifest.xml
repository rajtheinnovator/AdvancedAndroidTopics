<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.enpassio.advancedandroidtopics">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvpwithdaggermindorks.MvpWithDaggerActivity"
            android:label="Mvp with Dagger"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".daggerexamplebyharivignesh.DaggerExampleByHariVignesh"
            android:label="Dagger Example by Hari Vignesh"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".daggerexamplemindorks.view.DaggerExampleMindorks"
            android:label="Dagger Example Mindorks"
            android:parentActivityName=".MainActivity" />
    </application>

</manifest>